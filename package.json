{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "voting_dapp",
  "scripts": {
    "build": "npm run build",
    "prebuild": "npm run prebuild",
    "pretest": "npm run prebuild",
    "start": "npm start",
    "test": "npm test"
  },
  "type": "module"
}